<Window x:Class="FormatExcel.CompareResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FormatExcel"
        mc:Ignorable="d"
        Title="对比结果" Height="400" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Name="txtReportSummary"  Grid.Row="0" FontSize="14" Margin="5" TextWrapping="Wrap"  />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <GroupBox Header="PDF 文件夹" Grid.Column="0" Margin="5">
                <ListBox Name="lstPdfFiles" >
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="复制文本" Click="CopyPdfText_Click"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>
            </GroupBox>
            <GroupBox Header="DWG 文件夹" Grid.Column="1" Margin="5">
                <ListBox Name="lstDwgFiles">
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="复制文本" Click="CopyDwgText_Click"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>
            </GroupBox>
        </Grid>
    </Grid>
</Window>